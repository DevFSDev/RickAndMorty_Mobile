import { StyleSheet, ViewStyle } from "react-native";

export const style = StyleSheet.create({
    mainComponent : {
      width: 50,
      height: 50,
      backgroundColor: 'red',
      borderRadius: 25,
      borderColor: 'green',
      borderWidth: 5,
    } as ViewStyle,
  })
  